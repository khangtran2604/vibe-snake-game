/** Grid dimensions (20x20 squares) */
export const GRID_SIZE = 20;

/** Initial length of the snake */
export const INITIAL_SNAKE_LENGTH = 3;

/** Classic mode: starting speed in milliseconds per tick */
export const INITIAL_SPEED = 150;

/** Minimum speed (fastest) the game can reach in milliseconds */
export const MIN_SPEED = 50;

/** Points awarded for eating food */
export const POINTS_PER_FOOD = 10;

/** Classic mode: speed increase per threshold (Story 2.4) */
export const SPEED_INCREMENT = 5;

/** Classic mode: food count before speed increases (Story 2.4) */
export const FOOD_PER_SPEED_INCREASE = 3;

/** Speed mode: starting speed in milliseconds per tick */
export const SPEED_MODE_INITIAL = 100;

/** Speed mode: speed increase per threshold (Story 2.4) */
export const SPEED_MODE_INCREMENT = 8;

/** Speed mode: food count before speed increases (Story 2.4) */
export const SPEED_MODE_FOOD_THRESHOLD = 2;

/** Speed mode: minimum speed (fastest) in milliseconds per tick (Story 2.4) */
export const MIN_SPEED_MODE = 40;
